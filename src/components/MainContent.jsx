const MainContent = () => {
    return (
        <main className="flex items-center justify-center min-h-screen">
        <p className="text-xl text-gray-700 text-center">
            Ini adalah paragraf yang ada di tengah website.
        </p>
        </main>
    );
};

export default MainContent;